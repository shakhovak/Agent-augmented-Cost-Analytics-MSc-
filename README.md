# Cost Agent MVP

A dashboard and reporting agent for cost analysis and insights.

## Project Structure


## Setup
## Code quality (lint, format, tests)

Create/activate the venv, then run:

```bash
python -m ruff check .
python -m ruff check . --fix
python -m ruff format .
pytest
## Quickstart

# Outputs /Runs


