(
    """Core types, constants, and utilities."""
    """
Core shared types, enums, and utilities used across the project.

This package should not depend on конкретный data backend (CSV/Postgres).
"""
)
